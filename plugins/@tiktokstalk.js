let axios = require("axios");
let MessageType = require("@adiwajshing/baileys");
let { image } = MessageType
/*let handler = async (m, { conn, args, text }) => {
		if (text) return m.reply("Username nya tod?")
		await m.reply(global.wait)
	try{
        //let { user, stats } = await tiktod.getUserProfileInfo(text)
		let tod = `
*User ID:* ${user.id}
*Username:* ${user.uniqueId}
*Nickname:* ${user.nickname}
*Followers:* ${stats.followerCount}
*Following:* ${stats.followingCount}
*Posts:* ${stats.videoCount}
*Lopeh:* ${stats.heart}
`.trim()
		let res = await axios.get(user.avatarLarger)
		conn.sendMessage(from, res, image, { quoted: m, caption: tod })
	} catch (e) {
		m.reply("```Error```")
			console.log(e)
  }
}
					

handler.command = /^tiktokstalk$/i
handler.owner = false
handler.mods = false
handler.premium = false
handler.group = false
handler.private = false

handler.admin = false
handler.botAdmin = false

handler.fail = null
*/
//odule.exports = handler
